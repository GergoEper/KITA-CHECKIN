{{>navDetails}}

<p>This is the Child Profile</p>

{{#with childDetails}}
    <h1>Details for {{alias}}</h1>
    <p>Firstname: {{firstname}}</p>
    <p>Lastname: {{lastname}}</p>
    <p>Birthdate: {{birthdate}}</p>
	<a href="/child/{{_id}}/edit">Edit Childprofile ✏️</a>
	<a href="/child/{{_id}}/delete">Delete Childprofile ❌</a>
	{{!-- <form action="/child/{{_id}}/delete" method="GET">
        <label for="delete" id="delete">Delete Childprofile:</label>
        <input type="checkbox" name="delete" id="delete" values=true>
        <button type="submit"> ❌ </button> 
    </form> --}}
	<h3>Address: </h3>
	{{!-- {{#with author}}
		<p>{{name}} {{lastName}}</p>
		<p>Nationality: {{nationality}}</p>
		<img src="{{pictureUrl}}" alt="picture for {{lastName}}" height="200px">
	{{/with}} --}}
{{#each parent}}
<div>{{username}}</div>
<div>{{lastname}}</div>	

{{/each }}

	<p>Find a parent by username: </p>

<form action="/connectParent/{{_id}}" method="POST">
<label for="username">Username</label>
<input type="text" name="username" id="username">


<button type="submit">Connect a parent</button>
</form>
{{/with}}